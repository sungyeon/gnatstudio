# Build script for GNAT studio for macOS
1. install homebrew
2. install required packages
    * brew install python3
    * brew install gtk+3
    * brew install pygobject3
    * brew install autoconf
    * brew install automake
    * brew install autogen
    *
